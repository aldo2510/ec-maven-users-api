name: Test ARC Autopilot
on: { workflow_dispatch: {} }

jobs:
  demo:
    runs-on: arcautopilot
    container:
      image: ghcr.io/catthehacker/ubuntu:act-latest
    steps:
      - name: Hello from ARC Autopilot
        run: |
          echo "✅ ARC Autopilot ejecutando en GKE Autopilot"
          echo "Runner: $RUNNER_NAME"
          uname -a
